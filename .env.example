DATABASE_URL=postgres://mentions:mentions@localhost:5432/mentions
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
# Set this only when the public ingestion endpoint needs protection.
# INGEST_API_KEY=replace-with-a-long-random-value
